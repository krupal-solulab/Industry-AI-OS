apiVersion: v1
kind: ConfigMap
metadata:
  name: aios-config
  labels:
    {{- include "aios.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
